{
  "name": "1etiket-whatsapp-bot",
  "version": "2.0.0",
  "description": "1etiket.com.tr WhatsApp sipariş otomasyon botu (Evolution API + WhatsApp Web)",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "panel:dev": "cd panel && npm run dev",
    "panel:build": "cd panel && npm run build",
    "build": "npm run panel:build",
    "test": "jest --coverage",
    "test:watch": "jest --watch",
    "test:verbose": "jest --verbose --coverage"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.39.0",
    "@woocommerce/woocommerce-rest-api": "^1.0.1",
    "axios": "^1.6.0",
    "better-sqlite3": "^11.0.0",
    "dotenv": "^16.3.1",
    "express": "^4.18.2"
  },
  "devDependencies": {
    "jest": "^29.7.0",
    "nodemon": "^3.0.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
